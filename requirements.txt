# =============================================================
# LuxLLM - Requirements
# =============================================================

# -- Unsloth --
# pip install unsloth
unsloth
unsloth-zoo

# -- Core ML libraries (fine-tuning) --
torch>=2.1.0
transformers>=5.0.0
trl>=0.22.2
peft>=0.7.0
accelerate>=0.25.0
bitsandbytes>=0.41.0

# -- API clients (data generation & LLM-as-Judge scoring) --
openai>=1.0.0
python-dotenv>=1.0.0

# -- Dataset handling --
datasets>=2.14.0
pandas>=2.0.0

# -- Evaluation metrics (fine-tuning) --
evaluate>=0.4.0
rouge-score>=0.1.2

# -- Exam evaluation --
regex>=2023.0
python-Levenshtein>=0.21.0

# -- Configuration and utilities --
pyyaml>=6.0
tqdm>=4.65.0

# -- Logging and monitoring (optional but recommended) --
tensorboard>=2.14.0
wandb>=0.15.0  # Optional, for experiment tracking

# -- Indirect runtime dependencies --
# Required by various tokenizers and model loaders at runtime
numpy>=1.24.0
sentencepiece>=0.1.99
protobuf>=3.20.0
packaging>=23.0

# -- Development and testing (optional) --
# pytest>=7.4.0
# black>=23.0.0
# isort>=5.12.0
# flake8>=6.0.0